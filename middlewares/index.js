//const sanitazeData=require('./sanitize');
const accessByToken=require('./token');
const accessByRole=require('./role');
const validateRequest=require('./valid');
module.exports={
   // sanitazeData,
   accessByToken,
    accessByRole,
    validateRequest
}